eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
info:
  author: Douglas S. Nascimento
  mail: douglassnascimet@gmail.com
server:
  port: ${PORT:9999}
message: Mensagem generica do application.yml

#configura��o circuit breaker
resilience4j:
  circuitbreaker:
    instances:
      movie-service:
        allowHealthIndicatorToFail: false
        registerHealthIndicator: true
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 2
        failueRateThreshold: 10
        waitDurationInOpenState: 15000
        permittedNumberOfCalssInHalfOpenState: 1
        automaticTransitionFromOpenToHalfIfOpenEnabled: true

  #Configura��o do mecanismo de novas tentativas
  retry:
    instances:
      movie-service:
        maxAttempts: 3
        waitDuration: 100
        #retryExceptions[0]: feign.FeignExceptions$InternalServerError
        retryExceptions:
          - feign.FeignException$ServiceUnavailable
          - feign.FeignException$InternalServerError
          - org.springframework.web.client.HttpServerErrorException
          - java.util.concurrent.TimeoutException
          - java.io.IOException

#Configura��o Actuator
management:
  endpoints:
    web:
      exposure:
        include: circuitbreakers,circuitbreakerevents,health,info
  health:
    circuitbreakers:
      enabled: true
  endpoint:
    health:
      show-details: always
